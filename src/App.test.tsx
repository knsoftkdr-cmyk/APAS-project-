// Minimal test version
const TestApp = () => {
  return (
    <div style={{ padding: "20px", fontSize: "24px", fontWeight: "bold", color: "#000" }}>
      ✅ React is working! If you see this, the app can render.
    </div>
  );
};

export default TestApp;
